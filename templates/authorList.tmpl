{{ header "Authors" }}
            <h1>Authors</h1>
            <ul>
                {{ range . }}
                <li><a href="{{ domain }}/authors/{{ .Slug }}{{ index }}">
                    {{ .Name }} ({{ if eq .Birth 0 }}?{{ else }}{{ .Birth }}{{ end }}-{{ if eq .Death 0 }}?{{ else }}{{ .Death }}{{ end }})
                </a></li>
                {{ end }}
            </ul>
{{ footer }}
