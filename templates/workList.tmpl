{{ header "Works" }}
            <h1>Works</h1>
            <ul>
                {{ range . }}
                <li>
                    {{ if .Page }}<a href="{{ domain }}/works/{{ .Slug }}{{ index }}">{{ end }}{{ .Year }} - {{ .Authors }} - {{ .TitleMain }}{{ if .Page }}</a>{{ end }}
                </li>
                {{ end }}
            </ul>
{{ footer }}
