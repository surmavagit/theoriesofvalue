{{ header "Works" }}
            <main id="main">
            <h1>Works</h1>
            <ul id="worklist">
                {{ range . }}
                {{ workEntry . }}
                {{ end }}
            </ul>
            </main>
{{ footer }}
